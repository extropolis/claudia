# Server Configuration
PORT=3001

# =============================================================================
# SAP AI Core Configuration (for embedded Anthropic proxy)
# =============================================================================
# When configured, the backend will serve as an Anthropic API proxy at:
#   - GET  /v1/models     - List available models
#   - POST /v1/messages   - Anthropic Messages API
#
# This allows Claude Code to connect directly to this backend instead of
# requiring a separate sap-ai-proxy instance.

# SAP AI Core Authentication URL (OAuth token endpoint, e.g., https://xxx.authentication.xxx.hana.ondemand.com)
SAP_AICORE_AUTH_URL=

# SAP AI Core Client ID (OAuth client ID from service key)
SAP_AICORE_CLIENT_ID=

# SAP AI Core Client Secret (OAuth client secret from service key)
SAP_AICORE_CLIENT_SECRET=

# SAP AI Core Base URL (API endpoint, e.g., https://api.ai.xxx.aws.ml.hana.ondemand.com)
SAP_AICORE_BASE_URL=

# SAP AI Core Resource Group (deployment resource group, default: 'default')
SAP_AICORE_RESOURCE_GROUP=default

# Request timeout in milliseconds (default: 120000 = 2 minutes)
SAP_AICORE_TIMEOUT_MS=120000

# =============================================================================
# Legacy Configuration (for external proxy - deprecated)
# =============================================================================
# These are kept for backwards compatibility but are no longer needed
# if using the embedded proxy above.

# Provider ID for SAP AI Core (usually 'sap-ai-core')
SAP_AICORE_PROVIDER_ID=sap-ai-core

# Model ID to use (e.g., 'anthropic--claude-4.5-sonnet')
SAP_AICORE_MODEL_ID=anthropic--claude-4.5-sonnet

# LLM Service Configuration (for external SAP AI Proxy)
LLM_API_URL=http://localhost:3030/v1/chat/completions
LLM_MODEL=claude-sonnet-4-5-20250929
LLM_API_KEY=test-key-1

# =============================================================================
# Other Configuration
# =============================================================================

# Authentication Secrets (for future use)
JWT_SECRET=your-secret-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production

# OpenCode Configuration
OPENCODE_MODEL=anthropic--claude-4.5-sonnet
